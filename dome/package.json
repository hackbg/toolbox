{
  "name": "@hackbg/dome",
  "version": "2.0.0",
  "type": "module",
  "main": "dome.mjs",
  "types": "dome.d.ts",
  "files": ["dome.*"],
  "description": "DOM helper library. Create, compose, and query DOM elements quickly without syntactic sugar.",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "ensuite README.md || true",
    "cov": "ensuite-cov README.md || true",
    "ubik": "npm run check && npm run cov && ubik"
  }
}
