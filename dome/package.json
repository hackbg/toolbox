{
  "name": "@hackbg/dome",
  "version": "1.0.0",
  "type": "module",
  "main": "dome.mjs",
  "types": "dome.d.ts",
  "files": ["dome.*"],
  "description": "DOM helper library. Create, compose, and query DOM elements quickly and without syntactic hassle.",
  "devDependencies": {
    "@hackbg/ensuite": "^1.4.5"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "ensuite README.md || true",
    "cov": "ensuite-cov README.md || true",
    "ubik": "npm run check && npm run cov && ubik"
  }
}
